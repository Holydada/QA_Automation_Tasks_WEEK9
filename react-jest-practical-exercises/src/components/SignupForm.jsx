import {useState} from 'react'; export default function SignupForm({handleSubmit}){const [email,setEmail]=useState(''); function onSubmit(e){e.preventDefault(); handleSubmit(email);} return (<form onSubmit={onSubmit}><label htmlFor="email">Email</label><input id="email" name="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} /><button type="submit">Submit</button></form>);}
