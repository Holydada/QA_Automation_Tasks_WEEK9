import {add,subtract,multiply,divide} from '../utils/calculator'; describe('Simple Calculator',()=>{test('addition',()=>{expect(add(2,3)).toBe(5)}); test('subtraction',()=>{expect(subtract(7,2)).toBe(5)}); test('multiplication',()=>{expect(multiply(4,5)).toBe(20)}); test('division',()=>{expect(divide(10,2)).toBe(5)}); test('division by zero throws',()=>{expect(()=>divide(5,0)).toThrow('Cannot divide by zero')});});
