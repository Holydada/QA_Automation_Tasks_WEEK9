import {render,screen} from '@testing-library/react'; import Login from '../components/Login'; test('shows welcome when logged in',()=>{render(<Login isLoggedIn={true}/>); expect(screen.getByText('Welcome back!')).toBeInTheDocument();}); test('asks to log in when not logged in',()=>{render(<Login isLoggedIn={false}/>); expect(screen.getByText('Please log in.')).toBeInTheDocument();});
