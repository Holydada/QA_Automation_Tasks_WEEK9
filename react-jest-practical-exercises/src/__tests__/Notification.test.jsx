import {render,screen} from '@testing-library/react'; import Notification from '../components/Notification'; test('renders provided message',()=>{render(<Notification message="Hello there"/>); expect(screen.getByText('Hello there')).toBeInTheDocument();}); test('renders default when no message',()=>{render(<Notification/>); expect(screen.getByText('No notifications.')).toBeInTheDocument();});
