import {render,screen} from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import Counter from '../components/Counter'; test('initial value 0 and increments by 5 on click',async()=>{const user=userEvent.setup(); render(<Counter/>); const value=screen.getByLabelText('count'); expect(value).toHaveTextContent('0'); const btn=screen.getByRole('button',{name:/increment/i}); await user.click(btn); expect(value).toHaveTextContent('5'); await user.click(btn); expect(value).toHaveTextContent('10');});
